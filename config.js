// Defaults only — safe to commit. Copy `config.local.example.js` → `config.local.js` and add keys (not committed).
// Web3Forms access keys are domain-restricted in their dashboard but still trigger GitHub secret scanning if committed.
window.WEB3FORMS_KEY = "";
window.VAPI_PUBLIC_KEY = "";
window.VAPI_ASSISTANT_ID = "";
