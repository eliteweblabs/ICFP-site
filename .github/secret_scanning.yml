# Web3Forms access keys and Vapi public keys are vendor-documented *public* browser
# identifiers. GitHub secret scanning still matches them; orgs often cannot dismiss those
# alerts. Exclude ONLY this one file — never put server secrets or private API tokens here.
paths-ignore:
  - "config/vendor-public-client-ids.js"
