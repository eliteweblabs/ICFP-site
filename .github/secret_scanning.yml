# Web3Forms access keys are public browser identifiers; secret scanning still flags them.
# Exclude ONLY this file — never put private API tokens here.
paths-ignore:
  - "config/vendor-public-client-ids.js"
